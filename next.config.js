/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/notfall.ratingen.de',
    images: {
        unoptimized: true,
    },
};

module.exports = nextConfig;
